import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    weather: {
        color: '#fff',
        fontFamily: 'Raleway-Regular',
        fontSize: 17,
    },
    temperatures: {
        alignItems: 'center',
        marginTop: 12,
    }
});
